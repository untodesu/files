#!/bin/sh

# Being a "minimal" environment the i3 is,
# we have to set up the key mappings to change
# layouts and define layouts by yourselves, so
# this script comes in handy - I am bilingual.
setxkbmap -model pc105 -layout us,ru -option grp:win_space_toggle
